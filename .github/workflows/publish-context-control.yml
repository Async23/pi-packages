name: Publish context-control to npm

on:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

concurrency:
  group: publish-context-control
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Use an npm version with Trusted Publishing support
        run: npm install --global npm@11.17.0
      - name: Check package contents
        run: npm pack --workspace packages/context-control --dry-run
      - name: Publish package
        run: npm publish --workspace packages/context-control
